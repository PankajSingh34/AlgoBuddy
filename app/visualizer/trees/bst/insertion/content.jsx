"use client";
import ComplexityGraph from "@/app/components/ui/graph";
import { useEffect, useState } from "react";

const content = () => {
  const [theme, setTheme] = useState('light');
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    const updateTheme = () => {
      const savedTheme = localStorage.getItem('theme') || 'light';
      setTheme(savedTheme);
    };

    updateTheme();
    setMounted(true);

    window.addEventListener('storage', updateTheme);
    window.addEventListener('themeChange', updateTheme);

    return () => {
      window.removeEventListener('storage', updateTheme);
      window.removeEventListener('themeChange', updateTheme);
    };
  }, []);

  const paragraphs = [
    `Inserting a node into a Binary Search Tree follows a simple recursive process. Starting from the root, compare the value to be inserted with the current node. If the value is smaller, move to the left child; if larger, move to the right child. Repeat this process until you reach a null position, then insert the new node at that location. This maintains the BST invariant — all values in the left subtree are smaller than the root, and all values in the right subtree are larger.`,
    `Consider inserting the sequence [5, 3, 7, 2, 4, 6, 8] into an empty BST. First, 5 becomes the root. Next, 3 is smaller than 5, so it becomes the left child. Then 7 is larger than 5, becoming the right child. Continuing, 2 goes left of 3, 4 goes right of 3, 6 goes left of 7, and 8 goes right of 7. The resulting tree is balanced with 5 at the root.`,
    `The time complexity of BST insertion is O(h), where h is the height of the tree. In a balanced tree with n nodes, h = O(log n), making insertion efficient. However, in the worst case (e.g., inserting sorted data), the tree becomes a skewed chain with h = O(n). Self-balancing variants like AVL and Red-Black trees maintain logarithmic height by performing rotations after insertions.`,
    `Duplicate handling in BST varies by implementation. Some BSTs ignore duplicates, others store them in the left or right subtree consistently. The standard convention is to insert duplicates into the right subtree or reject them entirely. In this tutorial, we insert duplicates to the right.`,
  ];

  const working = [
    { points: "Start at the root. If the tree is empty, the new node becomes the root." },
    { points: "Compare the new value with the current node's value." },
    {
      points: "If smaller, move to the left child. If larger, move to the right child.",
      subpoints: [
        "If the child position is null, insert the new node there.",
        "If the child exists, repeat the comparison at that child.",
      ],
    },
    { points: "Return the (possibly new) root of the tree." },
  ];

  const complexity = [
    { data: "Best Case: Balanced tree → O(log n) comparisons" },
    { data: "Worst Case: Skewed tree → O(n) comparisons" },
    { data: "Average Case: Random insertions → O(log n)" },
  ];

  const algorithm = [
    { points: "If the tree (or subtree) is empty, insert the new node as root." },
    { points: "If value < current node's value, recursively insert in the left subtree." },
    { points: "If value > current node's value, recursively insert in the right subtree." },
    { points: "If value equals current node's value, handle duplicate (insert right or reject)." },
    { points: "Return the (unchanged) node pointer." },
  ];

  return (
    <main className="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-5 md:gap-4">
      <div className="col-span-1">
        <div className="hidden md:block">
          {mounted && (
            <iframe
              key={theme}
              src={
                theme === "dark"
                  ? "https://hw.glich.co/resources/embed/daily/dsa?theme=dark"
                  : "https://hw.glich.co/resources/embed/daily/dsa?theme=light"
              }
              width="100%"
              height="400"
              title="Daily DSA Challenge"
            ></iframe>
          )}
        </div>
        <div className="flex justify-center">
          <span className="text-xs hidden md:block">
            Daily DSA Challenge By{" "}
            <a
              href="https://hw.glich.co/resources/daily"
              target="_blank"
              className="underline hover:text-blue-500 duration-300"
            >
              Hello World
            </a>
          </span>
        </div>
      </div>
      <article className="col-span-4 max-w-4xl bg-white dark:bg-[#111] rounded-2xl border border-[#e5e7eb] dark:border-[#222] overflow-hidden mb-8">
        {/* What is BST Insertion */}
        <section className="p-6 border-b border-[#f3f4f6] dark:border-[#1e1e1e]">
          <h1 className="text-2xl font-bold text-[#1a1a1a] dark:text-white mb-4 flex items-center">
            <span className="w-1 h-6 bg-[#a435f0] mr-3 rounded-full"></span>
            What is BST Insertion?
          </h1>
          <div className="prose dark:prose-invert max-w-none">
            <p className="text-[#374151] dark:text-[#d1d5db] leading-relaxed">
              {paragraphs[0]}
            </p>
          </div>
        </section>

        {/* How Does It Work */}
        <section className="p-6 border-b border-[#f3f4f6] dark:border-[#1e1e1e]">
          <h1 className="text-2xl font-bold text-[#1a1a1a] dark:text-white mb-4 flex items-center">
            <span className="w-1 h-6 bg-[#a435f0] mr-3 rounded-full"></span>
            How Does It Work?
          </h1>
          <div className="prose dark:prose-invert max-w-none">
            <p className="text-[#374151] dark:text-[#d1d5db] mb-4 leading-relaxed">
              {paragraphs[1]}
            </p>

            <ol className="space-y-3 list-decimal pl-5 marker:text-gray-500 dark:marker:text-gray-400">
              {working.map((item, index) => (
                <li
                  key={index}
                  className="text-[#374151] dark:text-[#d1d5db] pl-2"
                >
                  {item.points}
                  {item.subpoints && (
                    <ul className="mt-2 space-y-2 list-disc pl-5 marker:text-gray-400 dark:marker:text-gray-500">
                      {item.subpoints.map((subitem, subindex) => (
                        <li
                          key={subindex}
                          className="text-[#6b7280] dark:text-[#9ca3af]"
                        >
                          {subitem}
                        </li>
                      ))}
                    </ul>
                  )}
                </li>
              ))}
            </ol>
          </div>
        </section>

        {/* Algorithm Steps */}
        <section className="p-6 border-b border-[#f3f4f6] dark:border-[#1e1e1e]">
          <h1 className="text-2xl font-bold text-[#1a1a1a] dark:text-white mb-4 flex items-center">
            <span className="w-1 h-6 bg-[#a435f0] mr-3 rounded-full"></span>
            Algorithm Steps
          </h1>
          <div className="prose dark:prose-invert max-w-none">
            <ol className="space-y-3 list-decimal pl-5 marker:text-gray-500 dark:marker:text-gray-400">
              {algorithm.map((item, index) => (
                <li
                  key={index}
                  className="text-[#374151] dark:text-[#d1d5db] pl-2"
                >
                  {item.points}
                </li>
              ))}
            </ol>
          </div>
        </section>

        {/* Time Complexity */}
        <section className="p-6">
          <h1 className="text-2xl font-bold text-[#1a1a1a] dark:text-white mb-4 flex items-center">
            <span className="w-1 h-6 bg-[#a435f0] mr-3 rounded-full"></span>
            Time Complexity
          </h1>
          <div className="prose dark:prose-invert max-w-none">
            <ul className="space-y-3 list-disc pl-5 marker:text-gray-500 dark:marker:text-gray-400">
              {complexity.map((item, index) => (
                <li
                  key={index}
                  className="text-[#374151] dark:text-[#d1d5db] pl-2"
                >
                  <span className="font-mono bg-[#f3f4f6] dark:bg-[#222] px-2 py-1 rounded-md text-sm">
                    {item.data.split(":")[0]}:
                  </span>
                  <span className="ml-2">{item.data.split(":")[1]}</span>
                </li>
              ))}
            </ul>

            <div className="mt-8">
              <ComplexityGraph
                bestCase={(n) => Math.ceil(Math.log2(n + 1))}
                averageCase={(n) => Math.ceil(Math.log2(n + 1))}
                worstCase={(n) => n}
                maxN={25}
              />
            </div>

            <div className="mt-6 p-4 bg-[#faf5ff] dark:bg-[#1a0a2e] rounded-xl border border-[#e9d5ff] dark:border-[#3b1a6e]">
              <p className="text-[#374151] dark:text-[#d1d5db] leading-relaxed">
                {paragraphs[2]}
              </p>
            </div>

            <div className="mt-4 p-4 bg-[#fff7ed] dark:bg-[#1a0f00] rounded-xl border border-[#fed7aa] dark:border-[#4a2d00]">
              <p className="text-[#374151] dark:text-[#d1d5db] leading-relaxed font-mono text-sm">
                {paragraphs[3]}
              </p>
            </div>
          </div>
        </section>
      </article>

      {/* Mobile iframe at bottom */}
      <div className="block md:hidden w-full">
        {mounted && (
          <iframe
            key={theme}
            src={
              theme === "dark"
                ? "https://hw.glich.co/resources/embed/daily/dsa?theme=dark"
                : "https://hw.glich.co/resources/embed/daily/dsa?theme=light"
            }
            width="100%"
            height="320"
            title="Daily DSA Challenge"
          ></iframe>
        )}
        <div className="flex justify-center pb-8">
          <span className="text-xs">
            Daily DSA Challenge By{" "}
            <a
              href="https://hw.glich.co/resources/daily"
              target="_blank"
              className="underline hover:text-blue-500 duration-300"
            >
              Hello World
            </a>
          </span>
        </div>
      </div>
    </main>
  );
};

export default content;
